# Database (host machine talking to docker-compose `db`: use port 5433 below)
DB_HOST=localhost
DB_PORT=5433
DB_USER=postgres
DB_PASS=postgres
DB_NAME=accounts_db

NODE_ENV=development
PORT=3000

JWT_SECRET=change_me_in_production

# Deposit cap (see plan/16-deposit-flow.md)
MAX_DEPOSIT=1000000
